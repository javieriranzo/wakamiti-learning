custom.define.query.parameters=los siguientes parámetros de búsqueda \\(con funciones\\):
custom.define.data.save=(que) se almacena el valor de la columna {column:word} de(l| la) * que satisface la siguiente cláusula SQL de la tabla de BBDD {table:word} como parámetro {text}:
custom.login=(que) el servicio usa autenticación con las credenciales {username:text}:{password:text}
custom.simplyDelay= esperamos {waitingSeconds:int} segundos para continuar